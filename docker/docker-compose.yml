version: '3.8'

services:
  kernel-builder:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    volumes:
      - ..:/workspace
    environment:
      - USE_CCACHE=1
    command: /bin/bash ./build.sh --skip-env
